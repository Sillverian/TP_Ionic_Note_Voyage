import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'tp_note_voyage',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
